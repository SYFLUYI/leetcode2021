package com.company;

public class Mybitmap {
    //每一个word是一个long类型元素，对应一个64位二进制数据
    private long[] words;
    //Bitmap的位数大小
    private int size;

    /**
     * 判断Bitmap某一位的状态
     * @param bitIndex 位图的第bitIndex位
     */

    /**
    * 把Bitmap某一位设置为true
    * /

     /**
     * 定位Bitmap某一位所对应的word
     */



     }

